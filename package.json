{
  "name": "securewebserver",
  "version": "1.0.0",
  "description": "'simple rest api for orderprocessing logic using JWT token base authentication'",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "'avesh katiyar'",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1"
  }
}
